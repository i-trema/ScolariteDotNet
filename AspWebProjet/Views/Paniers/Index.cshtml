@model IEnumerable<AspWebProjet.Models.Panier>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UtilisateurEmail">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UtilisateurEmail">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UtilisateurEmail">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
